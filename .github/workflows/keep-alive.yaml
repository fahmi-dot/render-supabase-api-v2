name: Keep-Alive

on:
  schedule:
    - cron: '*/14 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check endpoint
        run: curl -sS ${{ secrets.RENDER_SUPABASE_V2_URL }}/api/health > /dev/null
